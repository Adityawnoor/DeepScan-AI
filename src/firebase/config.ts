
/**
 * Firebase configuration object.
 * To ensure your training in Firebase Studio persists to Localhost:
 * 1. Go to your Firebase Console (console.firebase.google.com).
 * 2. Select your Project.
 * 3. Copy your Web App config and replace the placeholder values below.
 * 4. This ensures both environments hit the same "Neural Knowledge Base".
 */
export const firebaseConfig = {
  apiKey: "REPLACE_WITH_YOUR_API_KEY",
  authDomain: "REPLACE_WITH_YOUR_AUTH_DOMAIN",
  projectId: "REPLACE_WITH_YOUR_PROJECT_ID",
  storageBucket: "REPLACE_WITH_YOUR_STORAGE_BUCKET",
  messagingSenderId: "REPLACE_WITH_YOUR_MESSAGING_SENDER_ID",
  appId: "REPLACE_WITH_YOUR_APP_ID"
};
